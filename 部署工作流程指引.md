# 商业风险处置学习网站 — 部署工作流程指引

**版本**: v1.0  
**更新日期**: 2026-09-17  
**目标**: 将纯静态学习网站部署到 GitHub + Cloudflare Pages，实现双平台在线访问

---

## 一、项目概览

| 项目 | 说明 |
|------|------|
| 网站类型 | 纯静态 HTML/JS/CSS（无后端、无构建步骤） |
| 文章数量 | 86 篇（6 大栏目） |
| 站点入口 | `07_网站源码/risk-learning-site/index.html` |
| GitHub 仓库 | https://github.com/cmc005v-tech/risk-docs |
| 部署目标 | GitHub Pages + Cloudflare Pages |

---

## 二、仓库文件结构

```
risk-docs/                          ← GitHub 仓库根目录
├── .github/workflows/
│   └── deploy-pages.yml            ← GitHub Actions 部署工作流
── 07_网站源码/risk-learning-site/ ← 网站源码目录（部署源）
│   ├── index.html                  ← 入口文件
│   ├── style.css                   ← 样式
│   ├── app.js                      ← 应用逻辑
│   ├── data.js                     ← 数据调度器
│   ├── data-path.js                ← 学习路径数据
│   ├── data-theory.js              ← 理论课堂数据
│   ├── data-tools.js               ← 实操工具箱数据
│   ├── data-lab.js                 ← 沙盘训练数据
│   ├── data-cases.js               ← 案例库数据
│   ├── data-law.js                 ← 法规参考数据
│   ├── assets/                     ← 工具文件（xlsx）
│   ├── content/                    ← Markdown 源文件（55 个）
│   └── 使用指南.md
├── 风险处置学习网站-全量内容md-20260917/  ← 原始 md 素材
├── 01_计财管理认知/                ← 读书报告（docx）
├── 02_学习规划/                    ← 学习方案
├── 03_实操模板/                    ← 模板文件
├── 04_评估工具/                    ← 工具文件
├── 05_法规参考/                    ← 法规 PDF
├── 06_岗位认知方法论/              ← 顾问手册
├── P1_验收报告.md
├── P2_验收报告.md
── .gitignore
```

---

## 三、部署架构

```
┌─────────────────────────────────────────────────────────┐
│                    本地开发环境                           │
│  修改 content/*.md → 运行入站脚本 → 更新 data-*.js       │
│  修改 app.js / style.css → 本地 python -m http.server    │
│  验证通过后 → git commit + git push                      │
└──────────────────────┬──────────────────────────────────┘
                       │ git push
                       ▼
┌─────────────────────────────────────────────────────────┐
│                  GitHub 仓库 (main 分支)                  │
│  cmc005v-tech/risk-docs                                 │
│  触发 GitHub Actions → deploy-pages.yml                  │
└──────┬───────────────────────────────────────┬──────────┘
       │                                       │
       ▼                                       ▼
──────────────────┐              ┌──────────────────────┐
│   GitHub Pages   │              │   Cloudflare Pages   │
│  (备用/预览)      │              │   (主访问入口)        │
│  *.github.io     │              │   *.pages.dev        │
└──────────────────              └──────────────────────┘
```

---

## 四、部署步骤详解

### 步骤 1：启用 GitHub Pages

**目的**：让 GitHub Actions 工作流有权限部署到 Pages

**操作**：
1. 打开 https://github.com/cmc005v-tech/risk-docs/settings/pages
2. **Source** 下拉菜单 → 选择 **GitHub Actions**
3. 页面自动保存（无需点击 Save）

**验证**：页面显示 "GitHub Actions is currently used to deploy..."

---

### 步骤 2：确认 GitHub Actions 工作流

**工作流文件**：`.github/workflows/deploy-pages.yml`

**触发条件**：
- 推送到 `main` 分支时自动触发
- 也可在 Actions 页面手动触发（workflow_dispatch）

**部署路径**：`07_网站源码/risk-learning-site`（子目录部署）

**预期结果**：
- Actions 页面显示绿色 ✓
- 生成 GitHub Pages 访问链接

---

### 步骤 3：配置 Cloudflare Pages

**目的**：通过 Cloudflare CDN 加速访问，获得 `*.pages.dev` 域名

**操作**：
1. 登录 https://dash.cloudflare.com
2. 左侧导航 → **Workers & Pages**
3. 点击 **Create application** → **Pages** 标签 → **Connect to Git**
4. 授权 Cloudflare 访问 GitHub 仓库
5. 选择仓库：**cmc005v-tech/risk-docs**
6. 配置构建参数：

| 配置项 | 值 |
|--------|-----|
| Project name | `risk-docs`（或自定义） |
| Production branch | `main` |
| Build command | **留空**（纯静态站无需构建） |
| Build output directory | `07_网站源码/risk-learning-site` |
| Environment variables | 无需设置 |

7. 点击 **Save and Deploy**

**预期结果**：
- Cloudflare 自动克隆仓库并部署
- 生成访问链接：`https://risk-docs-xxxx.pages.dev`

---

### 步骤 4：（可选）绑定自定义域名

**操作**：
1. Cloudflare Pages 项目 → **Custom domains** → **Set up a custom domain**
2. 输入自定义域名（如 `risk.yourdomain.com`）
3. 按提示添加 DNS CNAME 记录
4. 等待 DNS 生效（通常几分钟）

---

## 五、日常更新流程

### 内容更新（新增/修改文章）

```
1. 修改 content/*.md 源文件
2. 运行入站脚本（Python）→ 更新 data-*.js
3. 本地验证：python -m http.server 8765 → 浏览器检查
4. git add . → git commit -m "更新内容：xxx" → git push
5. GitHub Actions + Cloudflare Pages 自动重新部署（约 1-2 分钟）
```

### 代码更新（修改 app.js / style.css）

```
1. 修改源码文件
2. 本地验证
3. git add . → git commit -m "优化：xxx" → git push
4. 自动部署
```

---

## 六、故障排查

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| GitHub Actions 报 404 | Pages 未启用 | Settings → Pages → Source 设为 GitHub Actions |
| Cloudflare 部署失败 | 构建路径错误 | 确认 Build output directory 为 `07_网站源码/risk-learning-site` |
| 网站打开白屏 | JS 语法错误 | 本地 `node -c data-*.js` 检查语法 |
| 文章不显示 | 数据文件未更新 | 重新运行入站脚本并 git push |
| Cloudflare 缓存旧版本 | CDN 缓存 | Cloudflare 控制台 → Purge cache |

---

## 七、关键链接

| 资源 | 链接 |
|------|------|
| GitHub 仓库 | https://github.com/cmc005v-tech/risk-docs |
| GitHub Actions | https://github.com/cmc005v-tech/risk-docs/actions |
| GitHub Pages 设置 | https://github.com/cmc005v-tech/risk-docs/settings/pages |
| Cloudflare 控制台 | https://dash.cloudflare.com |
| 本地开发服务器 | http://localhost:8765 |
